const validateSignInForm = (email, password, emailError, passError) => {
	const errors = {
		errorStatus: false,
		emailError: 'Email is required',
		passwordError: 'Password is required'
	}
}

// validate form, ca 41:00 i authentication part 1